// Current Date at top of page 
var CurrentDate = moment ();
$("#currentDay"). text(CurrentDate.format("dddd MMMM Do, YYYY"));

// Timeblocks 
  // Select the existing DOM element, <div class="container">, and assign to a new variable